open a command prompt or power shell for folders,

install npm and node.js before entering any command using npm 

front_end_server:
npm install (opening the server for the first time)
npm start

back_end_server:
npm install (opening the server for the first time)
node bd_server.js